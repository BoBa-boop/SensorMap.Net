<Window x:Class="SensorMap.View.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SensorMap.View"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:VM="clr-namespace:SensorMap.ViewModel"
        mc:Ignorable="d"
        Title="Карта датчиков" Height="450" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding ShowMenu}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                <GradientStop Color="#8d93c7" Offset="0.2"/>
                <GradientStop Color="#9bbac9" Offset="0.6"/>
                <GradientStop Color="#e6e9f2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ToggleButton Grid.Row="1" Width="100" Height="30" Command="{Binding }">
            <ToggleButton.Template>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border BorderBrush="White" BorderThickness="1" CornerRadius="10">
                        <Border.Background>
                            <SolidColorBrush Color="#5779c9" Opacity="1"/>
                        </Border.Background>
                        <TextBlock Text="Редактор" FontFamily="Tahoma"
                VerticalAlignment="Center"
                Foreground="PapayaWhip"
                FontSize="16" TextAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                            <Setter Property="Text" Value="Просмотр"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
        <ContentControl Grid.Row="0" Content="{Binding Navigation.CurrentView}"/>
        <TextBlock Text="Version:" Grid.Row="2"
                HorizontalAlignment="Right"/>
    </Grid>
</Window>

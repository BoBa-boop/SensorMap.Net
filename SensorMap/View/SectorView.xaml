<UserControl x:Class="SensorMap.View.SectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SensorMap.View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             xmlns:beh="clr-namespace:SensorMap.Behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:calc="clr-namespace:CalcBinding;assembly=CalcBinding"
             xmlns:viewmodel="clr-namespace:SensorMap.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:SectorsVM}"
             d:DesignHeight="450" d:DesignWidth="800" x:Name="root">
    <hc:TransitioningContentControl TransitionMode="Bottom2Top">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <hc:TextBox hc:InfoElement.Placeholder="Участок или механизация"
                        Margin="0,10,0,10" Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}"
                        HorizontalAlignment="Center" Width="300"/>

            <Grid Margin="10" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="300"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- ========= GALLERY: плитки секторов ========= -->
                <ListBox x:Name="SectorsListBox"
                         Grid.Column="0"
                         ItemsSource="{Binding Sectors}"
                         SelectedItem="{Binding SelectedSector}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingPanel.IsVirtualizing="True">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="240" Height="120" Margin="8"
                                    CornerRadius="8" Background="#F0F0F0"
                                    Cursor="Hand">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="6" Opacity="0.12" ShadowDepth="2"/>
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <ScaleTransform CenterX="120" CenterY="60"/>
                                </Border.RenderTransform>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.12"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.12"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.25" Duration="0:0:0.12"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.0" Duration="0:0:0.12"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.0" Duration="0:0:0.12"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.12" Duration="0:0:0.12"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="18" FontWeight="SemiBold"
                                               VerticalAlignment="Center" HorizontalAlignment="Center"
                                               TextWrapping="Wrap" Margin="12" LineHeight="22"/>
                                    <TextBlock Text="{Binding Mechanisms.Count, StringFormat={}{0} механизмов}"
                                               FontSize="12" Foreground="#888"
                                               VerticalAlignment="Bottom" HorizontalAlignment="Center"
                                               Margin="0,0,0,8"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#E8E8E8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- ========= DETAIL PANEL: правая панель ========= -->
                <Border Grid.Column="1" ClipToBounds="True">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Width" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDetailOpen}" Value="True">
                                    <Setter Property="Width" Value="1120"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border Background="#FAFAFA"
                            CornerRadius="8" BorderBrush="#DDD" BorderThickness="1"
                            Margin="10,0,0,0"
                            DataContext="{Binding SelectedSector}">
                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Header: название + кнопка закрытия -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="28" FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"/>
                                <Button Grid.Column="1" Content="✕" FontSize="18"
                                        Width="34" Height="34" Padding="0"
                                        Background="Transparent" BorderThickness="0"
                                        Foreground="#666" Cursor="Hand"
                                        VerticalAlignment="Top"
                                        Command="{Binding ElementName=root,Path=DataContext.ClosePanelCommand}"/>
                            </Grid>

                            <!-- Изображение механизма -->
                            <hc:ImageViewer Grid.Row="1"
                                            ImageSource="{Binding SelectedItem.Image, ElementName=MechListBox}"
                                            ShowImgMap="False" ShowToolBar="False"
                                            Margin="0,15,0,10"
                                            Height="380" Background="{StaticResource ColorPickerOpacityBrush}"/>

                            <!-- Список механизмов -->
                            <Grid Grid.Row="2" Margin="0,0,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Механизмы:" FontSize="15" FontWeight="SemiBold"
                                           Foreground="#555" Margin="0,0,0,6"/>
                                <ListBox x:Name="MechListBox" Grid.Row="1"
                                         ItemsSource="{Binding Mechanisms}"
                                         Background="#F5F5F5" BorderThickness="1"
                                         BorderBrush="#E0E0E0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold"
                                                       TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource ListBoxItemCustom}">
                                            <Setter Property="Background" Value="#EAEAEA"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="brd"
                                                                Background="{TemplateBinding Background}"
                                                                CornerRadius="5" Padding="10,6"
                                                                Margin="5" Cursor="Hand">
                                                            <Border.Effect>
                                                                <DropShadowEffect BlurRadius="6" Opacity="0.12" ShadowDepth="2"/>
                                                            </Border.Effect>
                                                            <Border.RenderTransform>
                                                                <ScaleTransform CenterX="0.5" CenterY="0.5"/>
                                                            </Border.RenderTransform>
                                                            <ContentPresenter/>
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Trigger.EnterActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.02" Duration="0:0:0.12"/>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.02" Duration="0:0:0.12"/>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.25" Duration="0:0:0.12"/>
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.EnterActions>
                                                                            <Trigger.ExitActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.0" Duration="0:0:0.12"/>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.0" Duration="0:0:0.12"/>
                                                                                        <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.12" Duration="0:0:0.12"/>
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.ExitActions>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background">
                                                                    <Setter.Value>
                                                                        <SolidColorBrush Color="{StaticResource BorderColor}" Opacity="0.5"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal" VirtualizingPanel.IsVirtualizing="True"/>
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                </ListBox>
                            </Grid>

                            <!-- Кнопка "Перейти" -->
                            <Button Grid.Row="3" Content="Перейти"
                                    Style="{StaticResource ButtonInfo}" Width="205" Height="40"
                                    Cursor="Hand" HorizontalAlignment="Right"
                                    Visibility="{Binding SelectedItem, ElementName=MechListBox, Converter={StaticResource Object2VisibilityConverter}}"
                                    Command="{Binding ElementName=root,Path=DataContext.GoToMech}"
                                    CommandParameter="{Binding SelectedItem, ElementName=MechListBox}"/>
                        </Grid>
                    </Border>
                </Border>
            </Grid>
        </Grid>
    </hc:TransitioningContentControl>
</UserControl>

<Window x:Class="SensorMap.View.SensorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SensorMap.View"
        mc:Ignorable="d"
        Title="Данные о датчике" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TreeView>
            <TreeViewItem Header="Датчики">
                <TreeViewItem Header="Оптические"/>
                <TreeViewItem Header="Индуктивные"/>
                <TreeViewItem Header="Барометрические"/>
                <TreeViewItem Header="Лазерные"/>
                <TreeViewItem Header="Инкрементальные"/>
                <TreeViewItem Header="Герконовые"/>
            </TreeViewItem>
        </TreeView>
        <GridSplitter/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border MinWidth="100" MinHeight="100" 
             HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image/>
            </Border>
            <GridSplitter/>
            <StackPanel Grid.Row="1">
                <GroupBox  Header="Описание" Height="70" Margin="5">
                    <TextBox Text="asdasdasdasdasdasdsa" IsReadOnly="True">
                        <TextBox.Background>
                            <SolidColorBrush Color="LightGray" Opacity="0.4"/>
                        </TextBox.Background>
                    </TextBox>
                </GroupBox>
                <WrapPanel Orientation="Horizontal" Width="auto" Height="auto">
                    <ItemsControl ItemsSource="{Binding Users}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5" Orientation="Horizontal">
                                    <TextBlock FontWeight="Medium" Text="{Binding Title}" />
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Text="{Binding Value}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
